import { createAction } from 'redux-actions';

export const addSet = createAction('ADD_SET');
export const editSet = createAction('EDIT_SET');
export const incrementSetRep = createAction('INCREMENT_SET_REP');
